[pytest]
markers =
    qt: exercises a Qt widget rather than a pure function
    network: calls a live service (api.geodatafarm.com); skips itself when
        that service is unreachable, so an offline run stays green
